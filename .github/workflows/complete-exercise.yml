name: Complete Exercise

on:
  push:
    branches:
      - copilot/exercise-introduction-to-github
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  actions: write

jobs:
  complete-exercise:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout my-first-branch
        uses: actions/checkout@v4
        with:
          ref: my-first-branch
          fetch-depth: 0

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Create PROFILE.md if it does not exist
        run: |
          if [ ! -f PROFILE.md ]; then
            echo "Welcome to my GitHub profile!" > PROFILE.md
            git add PROFILE.md
            git commit -m "Add PROFILE.md"
            git push origin my-first-branch
          else
            echo "PROFILE.md already exists"
          fi

      - name: Wait for Step 3 to be enabled (triggered by Step 2)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Waiting for Step 2 to process and enable Step 3..."
          for i in $(seq 1 12); do
            STATE=$(gh workflow view "Step 3" --json state --jq '.state' 2>/dev/null || echo "unknown")
            echo "Attempt $i/12: Step 3 state = $STATE"
            if [ "$STATE" = "active" ]; then
              echo "Step 3 is now enabled."
              break
            fi
            if [ "$i" -eq 12 ]; then
              echo "Step 2 did not enable Step 3 automatically. Enabling Step 3 directly..."
              gh workflow enable "Step 3" || true
            fi
            sleep 30
          done

      - name: Create pull request
        id: create-pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          EXISTING_PR=$(gh pr list --base main --head my-first-branch --json number --jq '.[0].number // empty')
          if [ -z "$EXISTING_PR" ]; then
            PR_URL=$(gh pr create \
              --base main \
              --head my-first-branch \
              --title "Add my first file" \
              --body "Adding my first file PROFILE.md to the repository as part of the Introduction to GitHub exercise.")
            PR_NUMBER=$(echo "$PR_URL" | grep -oE '[0-9]+$')
            echo "pr_number=$PR_NUMBER" >> $GITHUB_OUTPUT
            echo "Created PR #$PR_NUMBER"
          else
            echo "PR already exists: #$EXISTING_PR"
            echo "pr_number=$EXISTING_PR" >> $GITHUB_OUTPUT
          fi

      - name: Wait for Step 4 to be enabled (triggered by Step 3)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Waiting for Step 3 to process and enable Step 4..."
          for i in $(seq 1 12); do
            STATE=$(gh workflow view "Step 4" --json state --jq '.state' 2>/dev/null || echo "unknown")
            echo "Attempt $i/12: Step 4 state = $STATE"
            if [ "$STATE" = "active" ]; then
              echo "Step 4 is now enabled."
              break
            fi
            if [ "$i" -eq 12 ]; then
              echo "Step 3 did not enable Step 4 automatically. Enabling Step 4 directly..."
              gh workflow enable "Step 4" || true
            fi
            sleep 30
          done

      - name: Merge pull request
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ steps.create-pr.outputs.pr_number }}
        run: |
          if [ -n "$PR_NUMBER" ]; then
            echo "Merging PR #$PR_NUMBER..."
            gh pr merge "$PR_NUMBER" --merge
          else
            echo "No PR number found to merge"
            exit 1
          fi
